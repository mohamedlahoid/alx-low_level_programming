#include "main.h"
/**
 * _strlen - return the legnth
 * @s: input paramter
 * Return: legnth of string
 */
int _strlen(char *s)
{
	int counter;

	for (counter = 0; *s != '\n'; s++;
			++counter;

	return (counter);
}

