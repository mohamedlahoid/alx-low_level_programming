#include "main.h"
/**
 * _isupper - check if a letter is upper
 * @x: the number to be checked
 * Return: 1 for uperor 0 for else
 */
int _isupper(int x)
{
	if (x >= 65 && x <= 98)
	{
		return (1);
	}
	return (0);
}
